# TODO: generate justfile install script (template)

# TODO: install current package using generated justfile

#############################
# TODO: setup current package configs (e.g. alias, env vars, etc)
#############################
# install + set as default node version
# sames as : nvm alias default 20.14.0
nvm install 20.14.0 --default --latest-npm
